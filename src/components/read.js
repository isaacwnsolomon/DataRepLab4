// Arrow function 
const Read = () => {
    // Returns piece of text 
      return <h3>Hello from the read component!</h3>;

     
    };


    // Exporting so we can use it elsewhere 
    export default Read;